{
  "name": "mark-it",
  "version": "1.0.0",
  "description": "",
  "main": "app/init/main.js",
  "scripts": {
    "start": "electron .",
    "build": "esbuild app/renderer/index.js --bundle --platform=node --outdir=dist | electron .",
    "packtowin": "electron-packager . --platform=win32 --arch=x64"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-packager": "^17.1.1",
    "esbuild": "^0.16.17",
    "marked": "^4.2.5",
    "turndown": "^7.1.1"
  },
  "devDependencies": {
    "electron": "^22.0.1"
  }
}
